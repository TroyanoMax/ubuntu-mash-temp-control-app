<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MashTempApp" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkerColor</item>
        <item name="colorAccent">@color/accentColor</item>
        <item name="colorError">@color/warnColor</item>
    </style>

    <style name="Theme.MashTempApp" parent="Base.Theme.MashTempApp"/>
</resources>